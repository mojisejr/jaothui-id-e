# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# Database connection string for Supabase PostgreSQL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
DATABASE_URL="postgresql://user:password@host:5432/database?schema=public"

# Supabase Configuration
# Get these values from your Supabase project settings
SUPABASE_URL="https://your-project-ref.supabase.co"
SUPABASE_ANON_KEY="your-supabase-anon-key"

# Better Auth Configuration
# Generate a secure random string: openssl rand -base64 32
BETTER_AUTH_SECRET="your-better-auth-secret-key"
# Base URL of your application (for OAuth redirects)
BETTER_AUTH_URL="http://localhost:3000"

# LINE Login Configuration
# Get these values from your LINE Developers Console: https://developers.line.biz/
LINE_CLIENT_ID="your-line-client-id"
LINE_CLIENT_SECRET="your-line-client-secret"

# Next.js Environment
# Set to "production" in production, "development" in local dev
NODE_ENV="development"
